import 'package:flutter/material.dart';

class LinkedinAccountScheduler extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("ACCOUNT!"),
      ),
    );
  }
}
